import {
  checklist,
  recommendedOptions,
  timelineSteps,
} from '../../../shared/data/prototypeData';

export function useCaseJourney() {
  return {
    checklist,
    recommendedOptions,
    timelineSteps,
  };
}
